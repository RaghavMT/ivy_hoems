# Copy to .env.local for local development, and set the same two variables in the
# Vercel project settings. The API rejects the key as a query parameter; the app
# sends it in the X-API-Key header.
VITE_API_BASE_URL=
VITE_API_KEY=
