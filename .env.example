# Copy to .env and fill in. .env must be in .gitignore — never commit the key.
BASE_URL=https://api.example.homes
API_KEY=IVY26-XXXXXXXXXXXX

# From the registration email. Recorded in data/_manifest.json; Q5 is scoped to
# ASSIGNED_LOCALITY.
CITY=
ASSIGNED_LOCALITY=

# Optional
OUT_DIR=data
DELAY_SECONDS=0.15
